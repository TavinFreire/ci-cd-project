import { describe, it, expect } from "vitest";

describe("testes iniciais", () => {
    it("teste de soma", () => {
        expect(2 + 2).toBe(4);
    });
    it("teste de subtração", () => {
        expect(2 - 1).toBe(1);
    })
});